/** Stats. */
export interface Stats {

  /** Total amount of releases. */
  readonly releasesTotalAmount: number;

  /** Total amount of producers. */
  readonly producersTotalAmount: number;

  /** Total amount of characters. */
  readonly charactersTotalAmount: number;

  /** Total amount of tags. */
  readonly tagsTotalAmount: number;

  /** Total amount of visual novels. */
  readonly visualNovelsTotalAmount: number;

  /** Total amount of traits. */
  readonly traitsTotalAmount: number;
}
