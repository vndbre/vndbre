import type { FC } from 'react';
import { Layout } from 'src/components/Layout/Layout';

/** Profile overview page. */
const ProfileOverviewPage: FC = () => (
  <Layout>
    Profile overview page
  </Layout>
);

export default ProfileOverviewPage;
