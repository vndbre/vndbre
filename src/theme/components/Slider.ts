import { StyleConfig } from './types';

export const Slider: StyleConfig = {
  baseStyle: {
    thumb: {
      bg: 'orange.600',
      zIndex: 0,
    },
    filledTrack: {
      bg: 'orange.300',
    },
  },
};
