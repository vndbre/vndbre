import { StyleConfig } from './types';

export const Tag: StyleConfig = {
  sizes: {
    sm: {
      container: {
        borderRadius: 'sm',
      },
    },
    md: {
      container: {
        paddingY: 1,
        borderRadius: 'sm',
      },
    },
  },
};
