/**
 * Api endpoint paths.
 */
export enum ApiUrls {

  /** Vndb proxy endpoint. */
  Vndb = 'vndb',

  /** Tags api endpoint. */
  Tags = 'tags',

  /** Traits api endpoints. */
  Traits = 'traits',
}
